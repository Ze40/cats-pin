import { Header } from "@/widgets";

import ContentPageLayout from "../layout";

const FavoritePage = () => {
  return (
    <ContentPageLayout>
      <Header />
    </ContentPageLayout>
  );
};

export default FavoritePage;
